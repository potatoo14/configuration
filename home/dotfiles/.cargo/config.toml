# compile stuff as fast as possible
 
[target.x86_64-unknown-linux-gnu]
linker = "gcc"
rustflags = ["-Zshare-generics=y", "-C", "link-arg=-fuse-ld=mold"]

[unstable]
codegen-backend = true

[profile.dev]
codegen-backend = "cranelift"
